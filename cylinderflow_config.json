{
  "protocol": "cylinderflow.stride8.train75.eval65.v2",
  "method": "eagle",
  "noise_std": 0.02,
  "log_every_updates": 1,
  "checkpoint_every_updates": 1000,
  "training": {
    "main": {"epochs": 1000, "learning_rate": 0.0001, "microbatch": 1, "accumulation": 2, "validate_every_epochs": 50}
  }
}
